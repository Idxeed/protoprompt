# scripts/

Helper scripts for protoprompt development.

- `build_docs.py` — build and serve the bilingual (RU/EN) documentation.
- `build_telegram_demo_gif.py` — rebuild the Telegram README animation from
  `docs/assets/telegram-memory.png` (requires the `dev` extra).
